{
  "name": "@tuvi/ai-provider",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsc -w --noEmit",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'lint skipped'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "pino": "^9.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
