{
  "name": "@tuvi/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'lint skipped'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "@tuvi/ai-provider": "workspace:*",
    "@tuvi/db": "workspace:*",
    "bullmq": "^5.21.4",
    "pino": "^9.4.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
